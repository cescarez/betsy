<h2>Shop Stellar by Seller</h2>
<section>
  <% if @users.nil? %>
    <h2> Currently, there are no active sellers!</h2>
  <% else %>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.username, user_path(user.id)%>
      </li>
      <% end %>
  <% end %>
</section>
