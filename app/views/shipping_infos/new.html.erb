<%= render partial: "shipping-form", locals: {action_name: "Create"} %>
<section class="shipping buttons">
  <% if @order %>
    <%= button_to "Return to Shopping Cart", checkout_order_path(@order.id), method: :get %>
  <% end %>
</section>
